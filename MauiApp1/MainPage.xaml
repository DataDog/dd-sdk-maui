<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me"
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

            <Label
                Text="--- Test Features ---"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Margin="0,20,0,10" />

            <Button
                Text="Test HTTP Request"
                Clicked="OnTestHttpClicked"
                BackgroundColor="#2196F3"
                TextColor="White"
                HorizontalOptions="Fill" />

            <Button
                Text="Test Managed Exception"
                Clicked="OnTestManagedExceptionClicked"
                BackgroundColor="#FF9800"
                TextColor="White"
                HorizontalOptions="Fill" />

            <Button
                Text="Test Unobserved Task Exception"
                Clicked="OnTestUnobservedTaskExceptionClicked"
                BackgroundColor="#9C27B0"
                TextColor="White"
                HorizontalOptions="Fill" />

            <Button
                Text="CRASH APP (Native)"
                Clicked="OnCrashAppClicked"
                BackgroundColor="#F44336"
                TextColor="White"
                HorizontalOptions="Fill" />

            <Label
                x:Name="StatusLabel"
                Text=""
                HorizontalOptions="Center"
                Margin="0,10,0,0" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
